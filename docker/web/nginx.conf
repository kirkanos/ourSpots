server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Uploads von Handyfotos sind schnell einige Megabyte gross.
    client_max_body_size 30m;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # Gehashte Dateinamen: dauerhaft cachebar.
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Der Service Worker muss bei jedem Aufruf frisch geprueft werden, sonst
    # bleibt ein Update haengen, bis der alte Cache ablaeuft.
    location = /sw.js {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        expires off;
        try_files $uri =404;
    }

    location = /manifest.webmanifest {
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    location /api/ {
        # Eindeutiger Alias statt "api": im geteilten Traefik-Netz laufen
        # Container anderer Projekte, die ebenfalls "api" heißen können.
        proxy_pass http://ourspots-api:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # Bildskalierung und Nominatim-Wartezeit brauchen etwas Luft.
        proxy_read_timeout 120s;
    }

    # Single-Page-App: unbekannte Pfade liefern die App, nicht 404.
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }
}
